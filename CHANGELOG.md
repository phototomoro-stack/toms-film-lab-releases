# 変更履歴

各バージョン見出し（`## <バージョン>`）の下に箇条書きで変更点を書くと、
`Tools/release.sh` がその内容を GitHub リリースノートの
「このバージョンの変更点」セクションへ自動で載せます。見出しはビルドした
`Info.plist` の `CFBundleShortVersionString` と一致させてください。

## 0.1.8
- 初回起動時にチュートリアル（読み込み → 反転・調整 → プリセット → 書き出しの4ステップ）を表示。ツールバー右上の「？」またはヘルプ ▸ Tutorial… からいつでも見返せます（初回はスプラッシュの代わりに表示）
- カラーグレーディング: Shadows / Midtones / Highlights ホイールが輝度ゾーンに正しく限定されるように修正（従来は4ホイールとも画像全体に同じ色シフトがかかっていた）。効いていなかった Luminance スライダーも機能するように
- フィルムストリップ: 選択中より前のコマを閉じると選択が隣の写真にずれてしまう問題を修正
- 書き出し: DNG の選択肢を削除（実体は TIFF を .dng 拡張子で保存するだけの偽 DNG で、他ツールで開けない・誤認されるため。16-bit が必要な場合は TIFF 16-bit を）
- 読み込み: フォルダのドロップ / 選択に対応（フォルダ内の対応画像をフレーム順に一括読み込み）。読み込める画像が無いときは黙って無視せずメッセージを表示

## 0.1.7
- 書き出しサイズを選べるように（フル / 長辺ピクセル指定 / ％指定。単体・バッチ両方に適用、設定にも同じ項目）
- 単体書き出しに事前ダイアログを追加（サイズ・ファイル名・保存先フォルダを確認してから保存。前回のフォルダを記憶）
- 起動時にバージョンとアップデート状況を表示するスプラッシュを追加
- 画像を開く前のドロップ領域をプレビュー枠全体に拡大（どこにドラッグしてもOK。ドラッグ中は枠全体をハイライト）
- アプリメニューと設定に「応援する」（GitHub Sponsors）リンクを追加

## 0.1.6
- 表示言語の切替（日本語 / English）を追加（設定 →「言語」、再起動なしで即切替）
- 内蔵プリセットを「アンダー / 適正 / オーバー」の露出補正3種に刷新
- プリセットを1ファイルに全書き出しできるバックアップ機能を追加（アップデート前の保険に）
- プリセットにカーソルを合わせると本画像でライブプレビュー
- スポイト（WB / ベース色 / ニュートラル）に拡大ルーペ（RGB表示）と分かりやすいキャンセルを追加
- Before/After の分割線と実際の境界のズレを修正

## 0.1.5
- アップデートの入手を GitHub のリリースページ経由に一本化（アプリ内の直接ダウンロードを廃止し、展開エラーを回避）

## 0.1.4
- ダウンロードした zip が「展開できません（エラー94）」になる問題を修正（配布 zip の作り方を変更し、原因の属性を含めないように）

## 0.1.3
- 調整パネルの各セクションに「?」説明ボタンを追加（押すと、そのセクションで何がどう変わるかを表示）

## 0.1.2
- アプリ内「ダウンロード」で新バージョンを自動展開し、すぐ使える形（.app）で表示するように（Finder で「展開できない」問題を回避）

## 0.1.1
- プレビュー下部のツールバーをボタン化（ホバー／押下／オン状態が分かる見た目に刷新）
- ややこしかったスキャンの露出警告（黒つぶれ／白飛び）を削除

## 0.1.0
- 初回リリース
