# Tom's Film Lab — リリース / Releases

**Tom's Film Lab** は、スキャンしたフィルムネガを反転・編集するための macOS アプリです。
このリポジトリは配布用のビルド（バイナリ）を置く場所で、ソースコードは非公開で管理しています。

➡️ **[最新版をダウンロード（Download latest）](../../releases/latest)**

📖 **操作マニュアル** — [PDF 版](docs/TomsFilmLab-Manual-JA.pdf) / [ブラウザで読む](docs/MANUAL.md)
読み込みからネガ反転・調整・書き出しまで、画面の順に沿って解説しています（v0.2.7.1 時点）。

## 更新履歴 (Changelog)

各バージョンの変更点です。過去の全リリースは [Releases](../../releases) を参照してください。

<!-- CHANGELOG:START -->
### 0.2.7.2

- 修正：**「編集内容をコピー / 貼り付け」などの通知バナーが、画面上に残り続けることがある**不具合。バナーを続けて出したとき（コピーの直後に貼り付けた場合など）、2 通目以降の消去タイマーが動かず、そのまま出しっぱなしになっていました。バナーごとに 1.8 秒表示してから確実に消えるようにしました

### 0.2.7.1

ネガ反転の色補正まわりで、自動処理が先回りしすぎていた点を整理しました。

- **読み込み時にホワイトバランスを勝手に決めなくなりました。** 「色補正の初期モード＝オート（自動適用）」では、読み込んだ時点でベース色の推定・オレンジマスク除去に加えてホワイトバランスまで自動で決めていました。まだ何も触っていない写真なのに色温度・ティントが 0 でなくなり、あとから手で合わせるときも最初からアプリが決めた値が入っている状態でした。読み込み時はベース色とマスク除去までにして、ホワイトバランスは「自動」ボタンを押したときだけ動きます
- **一度決まったベース色が、自動で上書きされなくなりました。** これまでは「自動」ボタンを押すたびにベース色を推定し直していたため、フィルムの縁からスポイトで丁寧に取った値が、あとから押した「自動」で別の推定値に置き換わることがありました。ベース色欄の ✕（自動推定に戻す）で解除すれば改めて測り直せます
- **「ニュートラル」の取得を廃止しました。** マニュアルではベース色とニュートラルの両方を取るまで色補正がまったく適用されない作りで、ベース色だけを使うという選択ができませんでした。実際にはベース色だけのほうが素直に決まるため、ニュートラルの行とスポイトを外し、マニュアルはベース色を取った時点で効くようになります。すでにニュートラルを設定してある写真は、これまでどおりその値で表示されるので見た目は変わりません

### 0.2.7

コード全体を見直して、見つかった不具合をまとめて直しました。とくに**編集内容が保存されないことがある**問題が含まれるため、更新をおすすめします。

### 編集が失われる問題（最重要）

- 修正：**スライダーで調整した内容がディスクに保存されないことがある**不具合。写真ごとの編集を保存する仕組みが、編集された写真ではなく「そのとき選択されている写真」を保存しようとしていたため、調整してから別の写真に移ると、その調整が保存されないことがありました。保存のきっかけを写真自身が持つように作り直したので、どの操作経路から編集しても確実に保存されます
- 修正：**調整した直後（0.8 秒以内）にアプリを終了すると、その調整が消える**不具合。終了時に書き込み待ちの内容を必ず書き切ってから閉じるようにしました
- 修正：書き込みできない場所（読み取り専用のディスクイメージ、SD カード、共有フォルダなど）にある写真を編集すると、**保存に失敗しても何も表示されず、次に開いたときに初めて全部消えていることに気づく**問題。保存できないときはフォルダごとに一度だけ警告を出します
- 修正：同じフォルダに `frame01.tif` と `frame01.jpg` のように**拡張子違いの同名ファイルがあると、片方の編集がもう片方に上書きされる**不具合。編集ファイル（.tfl.json）の名前を `frame01.tif.tfl.json` のように元の拡張子を含む形に変更しました。これまでの編集はそのまま引き継がれ、古いファイルも消さずに残します
- 修正：編集内容のコピーで「すべて解除」を押しても、**カラーバランスだけ解除されない**不具合。カーブだけコピーしたつもりがカラーバランスも一緒に貼り付けられていました

### 取り消し（⌘Z）

- **「ロール全体に適用」「複数選択への貼り付け」「プリセットの一括適用」を ⌘Z 一回でまとめて取り消せる**ようになりました。これまでは1枚ずつしか戻せないのに、確認ダイアログには「⌘Z で取り消せます」と書かれていました
- 修正：**カーブの編集だけが ⌘Z で戻せない**不具合。他の調整と同じように、ドラッグ・点の削除・リセットが取り消せます
- 戻せる履歴がないとき、メニューの「取り消す」「やり直す」が淡色表示になるようになりました

### 現像結果

次の4つは、これまで表示と実際の動作が食い違っていたものです。**すでに編集した写真の見た目は変わりません** — 以前の方式のまま表示し続けます。新しい方式に切り替えたい写真には、調整パネルの上部に「新しい現像方式に更新」ボタンが出ます（⌘Z で戻せます）。

- 修正：**かすみ除去を少し上げただけで暗い部分が真っ黒に潰れる**不具合。計算式が 8bit 前提のまま別の色空間で評価されており、+30 で中間の明るさより暗い部分がすべて黒になっていました
- 修正：**カーブが描いたとおりに適用されない**不具合。どこに点を置いても内部的に5点へ置き換えられていたため、暗部を持ち上げる細かい調整などが反映されませんでした
- 修正：**自然な彩度（Vibrance）が彩度（Saturation）と同じ処理**になっていた不具合。本来の「彩度の低い色を優先的に上げる」動作になりました
- 修正：**周辺光量がトリミング前の中心に掛かる**不具合。中央から外れた位置で切り出すと、暗くなる場所が左右にずれていました
- 修正：**ホワイトバランスのスポイトが1回では合いきらない**不具合。同じ場所を何度も押すとだんだん近づく状態でした。強い色かぶりの場合、1回のクリックでほぼ完全に中和されるようになります（色補正の「オート」も同様に精度が上がります）

### 書き出し

- 修正：**トリミング中に書き出すと、トリミングも白枠も反映されない画像が出力される**不具合
- 修正：一括書き出しで、別フォルダにある同名の写真（`roll1/001.tif` と `roll2/001.tif` など）が**片方しか書き出されず、もう片方を黙って上書きしていた**不具合。出力先に同名ファイルがある場合も確認を挟むようになりました
- 一括書き出しの失敗理由が表示されるようになりました（これまではファイル名だけで、権限なのか容量なのか分かりませんでした）。失敗した枚数を「完了」に数えていた進捗表示も直しています
- **PNG の書き出しを 16bit に**変更。暗部の階調がなめらかになります
- 書き出し中にアプリが固まらなくなりました（単体・一括とも、書き出し処理が画面の更新を止めていました）

### 操作

- 修正：**ショートカットキーが、ロールに読み込んだ枚数ぶん実行される**不具合。36枚のロールで `[` を1回押すと36回転していました。ズームや反転も同様です
- 修正：ショートカット設定でキーの記録中に **Esc を押すと、キャンセルではなく割り当てが消える**不具合
- 修正：**トリミング枠の角がつかみにくい**不具合。つかめる範囲が見た目より右下にずれていました
- 修正：トリミング中にスポイトを起動すると、**両方の表示が重なって反転前の画像が透けて見える**不具合

### 表示・その他

- **メニューバーが日本語になりました**。これまで言語設定にかかわらず英語のままでした。「ここに画像をドロップ」など、起動直後に最初に見る画面の文字も日本語化しています
- 修正：英語表示にしていても、**アップデート通知の本文だけ日本語**で出る不具合
- 修正：EXIF のシャッター速度が **`1/0` や `1/499`** と表示・印字される不具合（長秒露光や 1/500 秒で発生）
- 修正：フィルムストリップで、**選択中の写真とそれ以外でサムネイルの明るさが違って見える**不具合
- Sigma（.x3f）や Phase One（.iiq）など一部の RAW が、**ドラッグでは読めるのに ⌘O では選べない**問題を解消
- 白枠キャプションを表示しているときのスライダー操作を軽くしました
- 大量の写真を読み込んだときにメモリが際限なく増え続けないようにしました

### 内部

- 今後この形式に項目を追加しても、既存の編集内容が失われないようにしました（カラーバランスや書き出し EXIF がまとめて初期値に戻る可能性がありました）

### 0.2.6.1
- 修正：EXIF 欄やスライダーの数値入力欄にカーソルがあるあいだ、**⌘⇧C / ⌘⇧V（編集設定のコピー / 貼り付け）や ⌘⇧R などのショートカットが効かなくなる**不具合。文字入力を邪魔しないためのキー素通し処理が、⌘ を伴うショートカットまで一緒に無効にしていました。1キーのショートカット（R / [ / ] / \）は従来どおり文字入力が優先されます
- 「貼り付け」「プリセット適用」「ロール全体に適用」でのサムネイル再生成を大幅に高速化。読み込み時のサムネ用デコード結果を保持するようになり、1コマあたり 200 ms 前後かかっていた RAW の再デコードがなくなりました
- 白枠 / EXIF 表記の「ロール全体に適用」を他の一括適用と同じ経路に統一。同時実行数の上限と進捗表示が付き、36 コマのロールで一斉にデコードが走って固まることがなくなりました
- 一括適用中の進捗表示の更新を間引き、適用そのものを軽くしました
- サイドカー（.tfl.json）の書き込みをメインスレッドから外し、一括適用の直後に操作が引っかかるのを解消しました

### 0.2.6
- 色補正「オート」の光源推定を作り直しました。これまでは画面の明るい部分を無彩色とみなして色を合わせていたため、夕景・紅葉・芝生・海など明部そのものが色づいているコマで画面全体が補色方向へ転ぶことがありました。新しい方式は画面全体から光源色を推定するのでこの転びが起きません（Kodak Gold 400 の1ロールで検証：良好なコマはほぼ変化なし、転んでいたコマの色かぶりが縮小）。保存済みの写真の見た目は変わりません
- 設定に「色補正の初期モード」を追加し、**マニュアル / オート / オート（自動適用）** から選べるようにしました。「オート（自動適用）」を選ぶと、読み込んだ時点でベース色推定 → オレンジマスク除去 → ホワイトバランスまで自動で適用され、フィルムストリップのサムネも補正後の見た目になります（初期値は従来どおり「オート」なので、更新しただけでは挙動は変わりません）
- ツールバーに **コミュニティ**（Discord）と **応援する**（GitHub Sponsors）のボタンを追加しました。どちらも既定のブラウザで開きます
- プリセット適用や「ロール全体に適用」でのサムネイル更新を高速化。すでに読み込み済みの写真は RAW を読み直さずにサムネを作り直すようになりました
- スライダー操作中の無駄な再描画を削減し、ドラッグの追従を軽くしました

### 0.2.5
- 「白枠 / EXIF 表記」と「書き出し EXIF」を並べ替えできるセクションに変更。他の調整セクションと同じようにドラッグで順番を変えられます（初期位置は従来どおり一番下）
- 「情報 / EXIF」セクションを廃止し、代わりにヒストグラムの上へ **ISO / 絞り / SS** を常時表示するバーを追加。クリックするとファイル名・撮影日時・カメラ・レンズ・サイズ・色空間などスキャン元の情報がその場で開きます
- 白枠キャプションの位置を下段（左下 / 下 / 右下）に整理しました。写真の上に重ねる中段と、タイトルのように見えてしまう上段は廃止しています（保存済みの設定は同じ左右位置の下段へ自動で移ります）
- キャプションの初期項目をカメラ・レンズ・フィルムに変更。焦点距離・絞り・シャッター・ISO は既定でオフになりました。フィルムスキャンではこれらがスキャナ由来の値のことが多く、そのまま焼き込まれるのを避けるためです（必要ならこれまでどおりチェックを入れて表示できます）
- 書き出し EXIF を編集すると、キャプション表示中はプレビューの白枠にも即座に反映されるように。「ロール全体に適用」でも同じく反映されます
- カラーバランスの Red / Green / Blue の ◀▶ の移動量を 2 に（1 クリックで色の変化が分かる幅に調整。⇧ Shift での微調整はそのまま）
- ネガ反転の Orange Mask「自動」ボタンを枠付きにして、ボタン全体をクリックできるように（UI サイズの設定にも追従します）

### 0.2.4
- 修正：「書き出し EXIF」で焦点距離を指定しても書き出した画像に反映されないことがある不具合。スキャン元の 35mm 換算焦点距離（`FocalLenIn35mmFilm`）がそのまま残っており、多くのビューアはそちらを表示していました。レンズ・絞りについても同様に、スキャン元の重複タグ（Aux のレンズ名・レンズ仕様・APEX 絞り値）が残って上書きが打ち消される場合がありました
- 焦点距離・絞りの入力で単位付きの表記（`50mm` / `f/2.8` / `F2.8`）も受け付けるように。これまでは数字以外が混ざると黙って無視されていました

### 0.2.3
- 書き出し画像に EXIF が入るようになりました（JPEG / PNG / TIFF 16-bit、単体・一括とも）。これまで書き出した画像には EXIF が一切残っていませんでした
- 右パネルに「情報 / EXIF」セクションを追加。スキャン元のファイル名・撮影日時・カメラ・レンズ・焦点距離・絞り・シャッター・ISO・サイズ・色空間を確認できます
- 右パネルに「書き出し EXIF」セクションを追加。メーカー・カメラ・レンズ・焦点距離・絞り・シャッター・ISO・撮影日時・フィルム・作者・著作権を自分で指定して書き出せます（空欄はスキャン元の値を継承／「ロール全体に適用」でまとめてコピー）
- カラーバランスを **WB / Mid / High / Shadow** の4タブに統合。ホワイトバランス（色温度・ティント・Red/Cyan）が同じセクションに入り、独立していた White Balance セクションと下部ツールバーの「WB Pick」は廃止しました（スポイトは WB タブへ移動）
- カラーバランスに全体の Red ⇄ Cyan 軸を追加。スライダーの範囲を ±100 に、◀▶ の移動量を 1 に調整
- パネルの並び順のデフォルトを「ネガ反転 → カラーバランス → 露出 →…」に変更。保存済みの並び順にも一度だけ自動で反映されます
- WB タブの Red / Cyan スライダーを左＝Red・右＝Cyan に反転し、Mid / High / Shadow と向きを統一（内部の値は変わらないため、保存済みの編集・プリセットの見た目は不変）
- 自動トリミングにフィルム判モード（35mm / 6×6 / 645 / 6×7）を追加。選んだ判の比率へ枠をスナップします（デフォルトは 35mm。縦位置スキャンは自動で縦横反転）
- トリミング中に **⇧ Shift** を押しながらハンドルをドラッグすると、その時点の縦横比を保ったままサイズ変更できるように（⌥ の中央基準と併用可）
- プリセットを既存と同じ名前で保存しようとすると、確認のうえ上書きするように。同じ名前のプリセットが2つできることはなくなりました
- 「現在の編集で上書き」が保存シートを開くようになり、保存する項目を選び直せるように（初期状態はそのプリセットの現在の選択を引き継ぎ）
- ネガ反転のデフォルトを 色補正＝マニュアル / Orange Mask＝1.00 に変更。「縁を補正から除外」は廃止しました
- 修正：編集のコピー／プリセット適用で Red/Cyan が引き継がれていなかった不具合

### 0.2.2
- ネガ反転に Orange Mask の「自動」を追加。フィルムのベース色を自動推定してオレンジマスクを除去し、ハイライトが無彩色になるよう自動でホワイトバランスまで仕上げます（フィルムの縁が写っているスキャンで特に効果的）
- 色補正に「オート / マニュアル」切り替えを追加。マニュアルではベース色とニュートラルを手動で取得でき、両方を選ぶまでは色補正を適用しません（未選択のあいだは案内を表示）
- プリセットの上書き保存に対応。プリセットライブラリの各項目メニュー、またはプリセットパネルの右クリックから「現在の編集で上書き」できます（名前と保存対象はそのまま、設定だけ差し替え）
- スライダーの当たり判定を見た目どおりのサイズに調整（0.2.1 で広げすぎた上下の余白を削除）

### 0.2.1
- 調整パネルのスライダーで、つまみが左端にあるときに左半分でつかめない不具合を修正。スライダー全幅のどこでも、またトラックの少し上下でもつかめるよう当たり判定を広げました
- 数値横の ◀ / ▶ ステッパーボタンの当たり判定を見た目より少しだけ広げ、押しやすくしました

### 0.2.0
- 自動トリミング（プロトタイプ）を追加。トリミング画面の「自動」ボタンで、像の範囲を自動検出してクロップ枠を提案します（Vision の矩形検出。余白のあるスキャンはフレームに合わせて提案、既にフチいっぱいのスキャンは提案なし）。検出結果はそのままハンドルで微調整できます
- キーボードの R でトリミングモードに入れるように（設定 → ショートカットで変更可）
- フィルムストリップで複数選択した写真を右クリックからまとめて一覧から外せるように（Remove from Roll の複数対応）
- 選択中の写真のサムネイルが常に見えるよう、フィルムストリップが自動でスクロールするように
- 調整パネルの初期の並び順を見直し
- 露出オーバーのハイライトに格子状のノイズ（モアレ）が出ることがあった問題を修正
- ショートカットキー（[ ] \ R など）を押したときに鳴っていたシステム警告音を抑制

### 0.1.9
- 全体的な動作を軽量化。プレビューを表示解像度で処理するようにし、スライダー操作やプリセットのホバープレビューの反応を大幅に改善（大きな画像ほど効果大。書き出しは従来どおりフル解像度で、画質は変わりません）
- 拡大表示では自動でフル解像度描画に切り替わるため、ピント確認も従来どおり
- 画像の一括読み込みとプリセットの一括適用を高速化（フィルムストリップのサムネイルを縮小デコードで生成。メモリ使用量も削減）
- ヒストグラムとサムネイルをプレビューの描画結果から直接更新するようにし、編集のたびに走っていた重複レンダリングを削減

### 0.1.8
- 初回起動時にチュートリアル（読み込み → 反転・調整 → プリセット → 書き出しの4ステップ）を表示。ツールバー右上の「？」またはヘルプ ▸ Tutorial… からいつでも見返せます（初回はスプラッシュの代わりに表示）
- カラーグレーディング: Shadows / Midtones / Highlights ホイールが輝度ゾーンに正しく限定されるように修正（従来は4ホイールとも画像全体に同じ色シフトがかかっていた）。効いていなかった Luminance スライダーも機能するように
- フィルムストリップ: 選択中より前のコマを閉じると選択が隣の写真にずれてしまう問題を修正
- 書き出し: DNG の選択肢を削除（実体は TIFF を .dng 拡張子で保存するだけの偽 DNG で、他ツールで開けない・誤認されるため。16-bit が必要な場合は TIFF 16-bit を）
- 読み込み: フォルダのドロップ / 選択に対応（フォルダ内の対応画像をフレーム順に一括読み込み）。読み込める画像が無いときは黙って無視せずメッセージを表示

### 0.1.7
- 書き出しサイズを選べるように（フル / 長辺ピクセル指定 / ％指定。単体・バッチ両方に適用、設定にも同じ項目）
- 単体書き出しに事前ダイアログを追加（サイズ・ファイル名・保存先フォルダを確認してから保存。前回のフォルダを記憶）
- 起動時にバージョンとアップデート状況を表示するスプラッシュを追加
- 画像を開く前のドロップ領域をプレビュー枠全体に拡大（どこにドラッグしてもOK。ドラッグ中は枠全体をハイライト）
- アプリメニューと設定に「応援する」（GitHub Sponsors）リンクを追加

### 0.1.6
- 表示言語の切替（日本語 / English）を追加（設定 →「言語」、再起動なしで即切替）
- 内蔵プリセットを「アンダー / 適正 / オーバー」の露出補正3種に刷新
- プリセットを1ファイルに全書き出しできるバックアップ機能を追加（アップデート前の保険に）
- プリセットにカーソルを合わせると本画像でライブプレビュー
- スポイト（WB / ベース色 / ニュートラル）に拡大ルーペ（RGB表示）と分かりやすいキャンセルを追加
- Before/After の分割線と実際の境界のズレを修正

### 0.1.5
- アップデートの入手を GitHub のリリースページ経由に一本化（アプリ内の直接ダウンロードを廃止し、展開エラーを回避）

### 0.1.4
- ダウンロードした zip が「展開できません（エラー94）」になる問題を修正（配布 zip の作り方を変更し、原因の属性を含めないように）

### 0.1.3
- 調整パネルの各セクションに「?」説明ボタンを追加（押すと、そのセクションで何がどう変わるかを表示）

### 0.1.2
- アプリ内「ダウンロード」で新バージョンを自動展開し、すぐ使える形（.app）で表示するように（Finder で「展開できない」問題を回避）

### 0.1.1
- プレビュー下部のツールバーをボタン化（ホバー／押下／オン状態が分かる見た目に刷新）
- ややこしかったスキャンの露出警告（黒つぶれ／白飛び）を削除

### 0.1.0
- 初回リリース
<!-- CHANGELOG:END -->

## インストール

1. 最新リリースから `.zip` をダウンロードして解凍します。
2. `TomsFilmLab.app` を「アプリケーション」フォルダへ移動します。
3. 下記「初回起動の許可」に従って、初回だけ実行を許可します。

## 初回起動の許可（重要）

このビルドは公証（notarization）されていないため、初回起動時に macOS の
Gatekeeper がブロックします。**一度だけ**次の操作で許可してください。

### 最新の macOS（Sequoia / 15 以降）

1. `TomsFilmLab.app` をダブルクリックする（「開けません」と表示されてOK）。
2. **システム設定 → プライバシーとセキュリティ** を開く。
3. 画面下の方に出る「**"TomsFilmLab" は…ブロックされました**」の欄の
   「**このまま開く**（Open Anyway）」ボタンを押す。
4. もう一度アプリを開き、確認ダイアログで「開く」を押す。

### それ以前の macOS

- `TomsFilmLab.app` を**右クリック →「開く」**→ 確認ダイアログで「開く」。

### うまくいかない場合（共通）

ターミナルで隔離属性を外すと確実に開けます：

```sh
xattr -dr com.apple.quarantine /Applications/TomsFilmLab.app
```

## アップデート

アプリは起動時にこのリポジトリの新しいリリースを自動で確認します
（設定 → **アップデート** →「今すぐ確認」でも手動確認できます）。
新しいバージョンがあれば通知され、その場でダウンロードできます。

---

## English (summary)

**Tom's Film Lab** is a macOS app for inverting and editing scanned film negatives.
This repo hosts release binaries only; the source is kept private.

**Install:** download the `.zip` from the [latest release](../../releases/latest) and
move `TomsFilmLab.app` to `Applications`. These builds are ad-hoc signed but **not
notarized**, so first launch needs a one-time approval:

- **macOS Sequoia (15)+**: double-click (it will be blocked) → **System Settings →
  Privacy & Security** → click **"Open Anyway"** for TomsFilmLab → open again and confirm.
- **Older macOS**: **right-click → Open**.
- Or clear quarantine: `xattr -dr com.apple.quarantine /Applications/TomsFilmLab.app`

The app checks this repo for updates from Settings → アップデート.
